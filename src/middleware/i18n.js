export default function ({ app, store, req, isHMR }) {
  // if (isHMR) return
  // let host
  // if (process.browser) {
  //   host = window.location.host
  // } else {
  //   host = req.headers['x-host'] || req.headers.host
  // }
  // const regex = new RegExp(`-(${store.state.locales.join('|')})\\.`)
  // const match = host.match(regex)
  // const locale = match ? match[1] : 'zh'
  // store.commit('setLang', locale)
  // app.i18n.locale = locale
}
